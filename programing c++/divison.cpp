using namespace std;
#include <iostream>

int main()

{

    int a;
    int b;
    int c;

    cout << "Enter two numbers ";

    cin >> a >> b;

    // division for the two numbers will be stored in the third variable c

    c = a / b;

    // printing the result for the same here

    cout << a << "/" << b << "= " << c;

    return 0;
}